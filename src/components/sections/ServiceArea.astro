<section class="section section--paper" id="service-area" aria-labelledby="area-title">
  <div class="container">
    <div class="service-area__inner">
      <div class="service-area__content">
        <span class="eyebrow">Where we work</span>
        <h2 id="area-title" class="section__title" style="margin-top:14px">Residential Electrician Serving SLO County</h2>
        <p class="section__lead" style="margin-top:18px; max-width:52ch">
          Licensed electrical work across SLO County and the Central Coast — from Paso Robles and Atascadero
          down through San Luis Obispo to Arroyo Grande and Santa Maria.
        </p>

        <div class="cities">
          <span class="city-chip city-chip--primary">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="6"/></svg>
            San Luis Obispo
          </span>
          <span class="city-chip">Atascadero</span>
          <span class="city-chip">Paso Robles</span>
          <span class="city-chip">Templeton</span>
          <span class="city-chip">Santa Margarita</span>
          <span class="city-chip">Arroyo Grande</span>
          <span class="city-chip">Santa Maria</span>
        </div>

        <p class="service-area__note">
          <strong>Not on the list?</strong> Give us a call. We travel for the right job.
        </p>
      </div>

      <div class="service-area__map" aria-hidden="true">
        <svg width="100%" viewBox="0 0 680 600" role="img">
          <title>Keif Electric service area map</title>
          <desc>Central Coast California showing Keif Electric's service area with coastline, Highway 101, mountains, and city locations.</desc>

          <!-- Mountains: fade in first -->
          <g class="map-mountains">
            <path d="M 180 80 L 200 140 L 220 100 L 240 160 L 260 120" stroke="#242021" stroke-width="1.2" fill="none" opacity="0.35"/>
            <path d="M 190 200 L 210 260 L 230 220 L 250 280 L 270 240" stroke="#242021" stroke-width="1.2" fill="none" opacity="0.35"/>
            <path d="M 200 340 L 220 400 L 240 360 L 260 420 L 280 380" stroke="#242021" stroke-width="1.2" fill="none" opacity="0.35"/>
            <path d="M 210 480 L 230 540 L 250 500" stroke="#242021" stroke-width="1.2" fill="none" opacity="0.35"/>
          </g>

          <!-- Coastline: draws in from north -->
          <path d="M 75 55 Q 78 95 82 135 Q 86 175 90 215" class="coastline-animated" stroke="#242021" stroke-width="2.5" fill="none"/>
          <path d="M 90 215 Q 108 245 128 270 Q 140 285 138 310" class="coastline-animated" stroke="#242021" stroke-width="2.5" fill="none"/>
          <path d="M 138 310 Q 115 335 100 360 Q 92 375 90 395" class="coastline-animated" stroke="#242021" stroke-width="2.5" fill="none"/>
          <path d="M 90 395 Q 88 420 92 445" class="coastline-animated" stroke="#242021" stroke-width="2.5" fill="none"/>
          <path d="M 92 445 Q 100 470 115 495 Q 125 515 128 545" class="coastline-animated" stroke="#242021" stroke-width="2.5" fill="none"/>
          <path d="M 128 545 Q 130 570 135 595" class="coastline-animated" stroke="#242021" stroke-width="2.5" fill="none"/>

          <!-- Highway 101: draws in after coastline -->
          <path class="hwy-draw" d="M 300 85 L 320 145 L 335 200 L 345 270 L 360 345 L 370 420 L 375 500" stroke="#242021" stroke-width="1.5" fill="none" opacity="0.3" stroke-dasharray="4 3"/>
          <text class="map-label-fade" style="animation-delay:1.8s" x="310" y="105" font-size="9px" opacity="0" fill="#242021">101</text>

          <!-- Ocean + region labels -->
          <text class="map-label-fade" style="animation-delay:2.8s" x="520" y="60" font-size="10px" fill="#242021" font-weight="500">Central Coast</text>
          <text class="map-label-fade" style="animation-delay:2.9s" x="520" y="80" font-size="10px" fill="#242021">California</text>
          <text class="map-label-fade" style="animation-delay:3.0s" x="45" y="320" font-size="9px" fill="#242021" text-anchor="middle" font-style="italic" opacity="0">Pacific Ocean</text>

          <!-- Coastal reference towns -->
          <text class="map-label-fade" style="animation-delay:3.1s" x="55" y="170" font-size="9px" fill="#242021">Cambria</text>
          <text class="map-label-fade" style="animation-delay:3.2s" x="135" y="315" font-size="9px" fill="#242021">Morro Bay</text>
          <text class="map-label-fade" style="animation-delay:3.3s" x="75" y="410" font-size="9px" fill="#242021">Los Osos</text>
          <text class="map-label-fade" style="animation-delay:3.4s" x="60" y="490" font-size="9px" fill="#242021">Pismo</text>

          <!-- Cities: staggered pop-in north to south -->
          <g class="city-group" style="animation-delay:2.8s">
            <circle cx="318" cy="135" r="2" fill="#242021" opacity="0.5"/>
            <text x="335" y="140" font-size="10px" fill="#242021" opacity="0.6">San Miguel</text>
          </g>

          <g class="city-group" style="animation-delay:3.0s">
            <circle cx="328" cy="185" r="3.5" fill="#242021"/>
            <text x="328" y="162" font-size="12px" font-weight="600" fill="#242021" text-anchor="middle">Paso Robles</text>
          </g>

          <g class="city-group" style="animation-delay:3.15s">
            <circle cx="355" cy="220" r="3.5" fill="#242021"/>
            <text x="370" y="224" font-size="11px" fill="#242021">Templeton</text>
          </g>

          <g class="city-group" style="animation-delay:3.3s">
            <circle cx="340" cy="280" r="3.5" fill="#242021"/>
            <text x="340" y="257" font-size="11px" fill="#242021" text-anchor="middle">Atascadero</text>
          </g>

          <g class="city-group" style="animation-delay:3.45s">
            <circle cx="395" cy="345" r="3.5" fill="#242021"/>
            <text x="410" y="349" font-size="11px" fill="#242021">Santa Margarita</text>
          </g>

          <!-- San Luis Obispo: primary city with pulsing ring -->
          <g class="city-group city-group--primary" style="animation-delay:3.6s">
            <circle class="atascadero-pulse" cx="360" cy="410" r="14" fill="none" stroke="#F3981F" stroke-width="1"/>
            <circle cx="360" cy="410" r="7" fill="none" stroke="#F3981F" stroke-width="1.5"/>
            <circle cx="360" cy="410" r="5" fill="#242021"/>
            <text x="360" y="385" font-size="14px" font-weight="600" fill="#242021" text-anchor="middle" letter-spacing="0.5px">San Luis Obispo</text>
          </g>

          <g class="city-group" style="animation-delay:3.75s">
            <circle cx="370" cy="490" r="3.5" fill="#242021"/>
            <text x="370" y="468" font-size="11px" fill="#242021" text-anchor="middle">Arroyo Grande</text>
          </g>

          <g class="city-group" style="animation-delay:3.9s">
            <circle cx="375" cy="540" r="3.5" fill="#242021"/>
            <text x="375" y="518" font-size="11px" fill="#242021" text-anchor="middle">Santa Maria</text>
          </g>
        </svg>
      </div>
    </div>
  </div>
</section>

<style>
  .section {
    padding: 120px 0;
    background: var(--ke-paper);
    border-top: 1px solid var(--border-1);
    border-bottom: 1px solid var(--border-1);
  }

  .container {
    max-width: var(--max);
    margin: 0 auto;
    padding-left: var(--gutter);
    padding-right: var(--gutter);
  }

  .service-area__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
  }

  .service-area__map {
    background: var(--ke-paper);
    border: 1px solid var(--border-1);
    border-radius: var(--radius-4);
    padding: 32px;
    overflow: hidden;
  }

  @keyframes drawCoastline {
    from { stroke-dashoffset: 1000; }
    to   { stroke-dashoffset: 0; }
  }

  @keyframes hwyDraw {
    from { stroke-dashoffset: 600; }
    to   { stroke-dashoffset: 0; }
  }

  @keyframes mountainFade {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  @keyframes cityPop {
    0%   { opacity: 0; transform: scale(0.4); }
    70%  { opacity: 1; transform: scale(1.12); }
    100% { opacity: 1; transform: scale(1); }
  }

  @keyframes labelFade {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  @keyframes pulse {
    0%   { opacity: 0.7; transform: scale(1); }
    50%  { opacity: 0.2; transform: scale(1.5); }
    100% { opacity: 0.7; transform: scale(1); }
  }

  .coastline-animated {
    stroke-dasharray: 1000;
    stroke-dashoffset: 1000;
    animation: drawCoastline 2.5s ease-in-out forwards;
    animation-delay: 0.3s;
  }

  .hwy-draw {
    stroke-dasharray: 600;
    stroke-dashoffset: 600;
    animation: hwyDraw 1.8s ease-in-out forwards;
    animation-delay: 1.2s;
  }

  .map-mountains {
    opacity: 0;
    animation: mountainFade 1s ease-out forwards;
    animation-delay: 0.1s;
  }

  .city-group {
    opacity: 0;
    transform-origin: center center;
    animation: cityPop 0.45s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
  }

  .map-label-fade {
    animation: labelFade 0.5s ease-out forwards;
  }

  .atascadero-pulse {
    transform-origin: 360px 410px;
    animation: pulse 2.4s ease-in-out infinite;
    animation-delay: 4s;
  }

  @media (prefers-reduced-motion: reduce) {
    .coastline-animated {
      animation: none;
      stroke-dashoffset: 0;
    }
    .hwy-draw {
      animation: none;
      stroke-dashoffset: 0;
    }
    .map-mountains {
      animation: none;
      opacity: 1;
    }
    .city-group {
      animation: none;
      opacity: 1;
    }
    .map-label-fade {
      animation: none;
      opacity: 1;
    }
    .atascadero-pulse {
      animation: none;
    }
  }

  .section__title {
    font-weight: 900;
    font-size: clamp(34px, 4vw, 54px);
    line-height: 1.04;
    letter-spacing: -0.02em;
    margin: 14px 0 0;
    color: var(--ke-ink);
    max-width: 18ch;
  }

  .section__lead {
    font-size: 17px;
    line-height: 1.55;
    color: var(--fg-2);
    max-width: 48ch;
    margin: 0;
    font-weight: 500;
  }

  .eyebrow {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 12px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ke-orange);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .eyebrow::before {
    content: "";
    width: 24px;
    height: 2px;
    background: var(--ke-orange);
    display: inline-block;
    margin-right: 8px;
  }

  .cities {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 28px;
  }

  .city-chip {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 16px;
    background: var(--ke-paper);
    border: 1px solid var(--border-1);
    border-radius: var(--radius-pill);
    font-size: 14px;
    font-weight: 600;
    color: var(--ke-ink);
    transition: background var(--dur-1) var(--ease-out), border-color var(--dur-1) var(--ease-out), color var(--dur-1) var(--ease-out);
  }

  .city-chip svg {
    width: 13px;
    height: 13px;
    color: var(--ke-orange);
  }

  .city-chip--primary {
    background: var(--ke-ink);
    color: var(--ke-bone);
    border-color: var(--ke-ink);
  }

  .city-chip--primary svg {
    color: var(--ke-amber);
  }

  .city-chip:hover {
    border-color: var(--ke-orange);
    color: var(--ke-orange);
  }

  .city-chip--primary:hover {
    background: var(--ke-orange);
    color: #fff;
    border-color: var(--ke-orange);
  }

  .service-area__note {
    margin-top: 24px;
    font-size: 14.5px;
    color: var(--fg-2);
    max-width: 48ch;
  }

  .service-area__note strong {
    color: var(--ke-ink);
    font-weight: 700;
  }

  @media (max-width: 768px) {
    .service-area__inner {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .section {
      padding: 72px 0;
    }

    .service-area__map svg {
      max-height: 340px;
    }
  }
</style>
